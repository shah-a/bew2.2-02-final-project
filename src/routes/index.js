const home = require('./home');
const auth = require('./auth');
const users = require('./users');
const locations = require('./locations');
const adhan = require('./adhan');

module.exports = {
  home, auth, users, locations, adhan
};
